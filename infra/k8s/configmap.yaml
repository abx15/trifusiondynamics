apiVersion: v1
kind: ConfigMap
metadata:
  name: agencyos-config
  namespace: default
data:
  NODE_ENV: "production"
  PORT: "3000"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  ENVIRONMENT: "production"
